name: Release Windows EXE

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Install build dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller

      - name: Build EXE (windowed, no console)
        run: |
          pyinstaller --noconfirm --clean --windowed --name jiaoyisuopiliang main.py

      - name: Package ZIP
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Path release -Force | Out-Null
          $zipName = "jiaoyisuopiliang-${{ github.ref_name }}-windows-x64.zip"
          Compress-Archive -Path "dist/jiaoyisuopiliang/*" -DestinationPath "release/$zipName" -Force

      - name: Upload workflow artifact
        uses: actions/upload-artifact@v6
        with:
          name: jiaoyisuopiliang-windows
          path: release/*.zip

      - name: Publish GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          $tag = "${{ github.ref_name }}"
          $assets = Get-ChildItem "release/*.zip" | ForEach-Object { $_.FullName }

          gh release view $tag *> $null
          if ($LASTEXITCODE -eq 0) {
            gh release upload $tag $assets --clobber
          } else {
            gh release create $tag $assets --generate-notes
          }
